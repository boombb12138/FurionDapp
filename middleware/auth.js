let list = ['/login'];

export default async ({ app, store, route, redirect, req }) => {
  // let host = '';
  // if (process.server) {
  //   host = req.headers.host;
  // } else {
  //   host = window.location.host;
  // }
  // let token = store.state.user.token;
  // if (list.includes(route.path)) {
  //   return;
  // }
  // if (!token) {
  //   redirect('/login');
  // }
};
